# Functions

## Scope of Study

- Built-in functions against user-defined functions
- `print()`, `input()`, `int()` and `range()` as functions already in use
- `def`, the function name, the parentheses, the colon, the indented body
- Calling a function
- Why a definition on its own runs nothing
- The `pass` statement, since a function body cannot be empty
- Docstrings

---

## Authoring Status

Instructional content for this chapter is not yet written. The scope above defines what this file must cover.
